const routes = [
  {
    path: '/',
    exact: true,
    component: 'Home'
    // loadData: () => [
    // ]
  },
  {
    path: '/about',
    exact: true,
    component: 'About'
  }
];

export default routes;
