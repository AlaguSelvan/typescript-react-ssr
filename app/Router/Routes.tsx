import * as React from 'react'

const routes = [
	{
		path: '/',
		exact: true,
		page: 'Home',
		// loadData: () => [
		// ]
	},
	{
		path: '/about',
		exact: true,
		page: 'About'
	}
]

export default routes